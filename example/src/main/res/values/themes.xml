<resources>
    <!-- Minimal window theme required by the Android system.
         All visual theming is handled by SampleTheme (Material3) in Compose. -->
    <style name="Theme.ExampleApplication" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorPrimary">@color/sap_blue</item>
        <item name="android:statusBarColor">@color/sap_blue</item>
    </style>
</resources>
